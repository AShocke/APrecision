#include <iostream>
#include <vector>
#include <iostream>
#include "precision.h"


int main() {
	std::string s;
	while (s != "quit") {
		printf("Enter a number of arbitrary precision.\n");
		printf(">> ");
		std::cin >> s;
		s.
		if (s ! = )
			real_precision rp_a (s);
			rp_a.print();
		rp_a.print_to_string();
	
	}
}

